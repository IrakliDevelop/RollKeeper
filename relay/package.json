{
  "name": "rollkeeper-battlemap-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "vitest run",
    "type-check": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@fieldnotes/core": "0.46.1",
    "@fieldnotes/sync": "0.7.0",
    "@fieldnotes/sync-server": "0.8.0",
    "redis": "^4.7.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
